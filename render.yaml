services:
  - type: web
    name: flask-transcriber-ui
    env: python
    plan: free
    branch: main
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn backend:app --worker-class uvicorn.workers.UvicornWorker --bind 0.0.0.0:$PORT"
    plan: free

